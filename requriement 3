def get_video_details(video_path):

    cap = cv2.VideoCapture(video_path)

    fps = cap.get(cv2.CAP_PROP_FPS)

    total_frames = int(cap.get(cv2.CAP_PROP_FRAME_COUNT))

    width = int(cap.get(cv2.CAP_PROP_FRAME_WIDTH))

    height = int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT))

    duration = total_frames / fps if fps > 0 else 0

    cap.release()

    return width, height, fps, duration, total_frames

width, height, fps, duration, frames = get_video_details(save_path)

st.write("Resolution :", width, "x", height)

st.write("FPS :", round(fps,2))

st.write("Duration :", round(duration,2), "seconds")

st.write("Total Frames :", frames)
